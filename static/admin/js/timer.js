var test = function(){
   /* var timeID;
    var keyid = $("div#class-management-wrapper div:first").attr("id")
    if(keyid == "Edit"){
        //$.tip("开始计时")
        timeID = window.setInterval(save,1000)
    }else if(keyid != "Edit"){
        //$.tip("停止计时");
        window.clearInterval(timeID)           
    }*/
}
//var timer = setInterval(test,3000)

function save(){
    $.tip("save")
}